/**
 * @file scheduling.cpp
 * @author Thomas Laughridge (tcl5tu@virginia.edu)
 * @brief 
 * @version 0.1
 * @date 2022-12-04
 * 
 */

#include <iostream>
#include <string>

using namespace std;

void readInput() {
    int numRequests, numCourses, numEnrolled = 0;
    cin >> numRequests >> numCourses >> numEnrolled;

    for (int i = 0; i < numRequests; i++) {
        string name, course;
        cin >> name >> course;
    }
}

int main() {

}